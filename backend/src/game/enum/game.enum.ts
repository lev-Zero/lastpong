export enum gameStatus {
  WAITINGPLAYER,
  GAMESTART,
  COUNTDOWN,
  GAMEPLAYING,
  GAMEOVER,
}

export enum BackgroundColor {
  DEFAULT,
  BLUE,
  ORANGE,
}

export enum Mode {
  NONE,
  SPEEDUPBALL,
  SPEEDDOWNBALL,
  SIZEUPBALL,
  SIZEDOWNBALL,
  SIZEUPTOUCHBAR,
  SIZEDOWNTOUCHBAR,
}

export enum PlayerType {
  PLAYER,
  SPECTATOR,
}

export enum GameReadyStatus {
  WAITINGOTHERPLATER,
}
